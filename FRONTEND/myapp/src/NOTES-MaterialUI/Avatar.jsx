
// Q. How to create Letter icons for a list of users.

// function stringToColor() {
//     const r = Math.floor(Math.random() * 256);
//     const g = Math.floor(Math.random() * 256);
//     const b = Math.floor(Math.random() * 256);
//     return `rgb(${r}, ${g}, ${b})`;
// }

// <Avatar sx={{ bgcolor: stringToColor(), width: 40, height: 40, mr: 2 }} >
// {row.client.split(' ')[0][0]}{row.client.split(' ')[1][0]} 
// </Avatar>

// row.client is mapping part ---- row.client.split to remove the Initials of the user
// split(' ')[0][0] takes first word & it's first letter
// split(' ')[1][0] takes second word & it's first letter

// The space character (' ') is used in the split method to split the full name into individual words.
// For example, if row.client is "John Doe", the split method would return an array ["John", "Doe"].
// We can then access the first name and last name using array indices ([0] and [1], respectively).






















